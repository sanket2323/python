🧠 Level 1: Basics & Foundations

    Find the maximum and minimum element in an array.

    Reverse an array in-place.

    Check if an array is a palindrome.

    Rotate the array to the right by k steps.

    Find the index of a given element. If not present, return -1.

    Count the number of even and odd elements.

    Remove duplicates from a sorted array (in-place).

    Find the second largest element in the array.

⚙️ Level 2: Logic Building & Intermediate

    Move all zeroes to the end without changing the order of non-zero elements.

    Find the maximum sum of a contiguous subarray (Kadane's Algorithm).

    Check if two arrays are equal (after sorting).

    Find common elements in three sorted arrays.

    Find the majority element (appears more than n/2 times).

    Calculate the running sum of the array.

    Check if there exists a pair with a given sum.

🚀 Level 3: Advanced Thinker

    Find all subarrays with sum equal to 0.

    Find the longest subarray with equal number of 0s and 1s.

    Product of array except self (without using division).

    Trapping Rain Water Problem.

    Maximum sum subarray of size k (sliding window technique).